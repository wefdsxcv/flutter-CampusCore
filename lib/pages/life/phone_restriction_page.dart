import 'package:flutter/material.dart';

class PhoneRestrictionPage extends StatelessWidget {
  const PhoneRestrictionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('質問箱')),
      body: const Center(child: Text('質問箱ページ（開発中）')),
    );
  }
}